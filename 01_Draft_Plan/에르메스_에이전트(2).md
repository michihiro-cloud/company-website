[26. 6. 20. 오후 2:07] hermes: 📋 1. 프로젝트 개요  
**목표** –  
- **회사 홈페이지**(제품·서비스 소개, 포트폴리오, 연락처 등) 를 **VS Code** + **Claude Code** 로 코딩하고, **Obsidian Vault** 로 기획·콘텐츠·RAW 데이터를 관리한다.  
- 모든 파일을 **GitHub**에 올려서 CI/CD(자동 배포) 를 구현한다.  
- **초보자**도 따라 할 수 있도록 단계별 가이드와 샘플 코드를 제공한다.  
**핵심 흐름**  
1. **Obsidian**에 기획·콘텐츠·이미지·데이터(마크다운) → 로컬 폴더에 저장  
2. 동일 폴더를 **VS Code** 로 열어 **Claude Code**에게 “이 마크다운을 HTML/React 로 구현해줘” 라는 프롬프트 전송 → 코드 자동 생성  
3. 생성된 코드를 **Git** 으로 커밋 → **GitHub**에 푸시 → **Vercel/Netlify** 로 자동 배포  
📁 2. 폴더·파일 구조 (Obsidian Vault ↔ VS Code)  
my-company-website/ # ★ VS Code + Git 루트 / Obsidian Vault 루트  
│  
├─ .github/ # GitHub Actions (CI/CD) 설정  
│ └─ workflows/  
│ └─ deploy.yml  
│  
├─ docs/ # Obsidian에 쓰는 원본 마크다운 (기획·콘텐츠)  
│ ├─ 00_개요.md  
│ ├─ 01_회사소개.md  
│ ├─ 02_서비스_스테레오3D.md  
│ ├─ 03_기술스택.md  
│ ├─ 04_포트폴리오.md  
│ └─ 05_연락처.md  
│  
├─ public/ # 정적 파일 (이미지·favicon·favicon.ico 등)  
│ └─ images/  
│ ├─ logo.png  
│ └─ hero.jpg  
│  
├─ src/ # Claude Code가 생성·수정할 코드  
│ ├─ components/  
│ │ ├─ Header.jsx  
│ │ ├─ Footer.jsx  
│ │ └─ ServiceCard.jsx  
│ ├─ pages/  
│ │ └─ index.jsx  
│ └─ App.jsx  
│  
├─ .gitignore  
├─ README.md  
├─ package.json  
├─ vite.config.js # Vite(또는 Astro, Next.js 등) 설정  
└─ obsidian-sync.sh # Obsidian ↔ VS Code 동기화 스크립트 (선택)  
- **Obsidian** → `docs/` 폴더에 마크다운을 그대로 저장합니다.  
- **VS Code**는 같은 루트를 열어(`my-company-website`) 작업하므로 두 툴이 **실시간으로 같은 파일을 공유**하게 됩니다.  
**Tip**: Obsidian 플러그인 _Obsidian Git_ 을 켜두면 `docs/` 폴더를 자동 커밋/푸시 할 수 있어 언제든 최신 기획을 GitHub에 반영할 수 있습니다.  
🛠️ 3. 필수 도구 설치  
도구 | 설치 방법 (터미널) | 비고  
**Node 14 이상** | `curl -fsSL https://deb.nodesource.com/setup_20.x \| sudo -E bash - && sudo apt-get install -y nodejs` | Vite·React·Astro 등  
**VS Code** | `sudo snap install code --classic`  
**Claude Code CLI** | `npm i -g @anthropic/claude-code` 또는 VS Code Marketplace → “Claude for VS Code” 설치 | `claude` 명령어 사용  
**Git** | `sudo apt install git`  
**GitHub CLI** (선택) | `sudo apt install gh` | 리포 생성·푸시 간편  
**Obsidian** | https://obsidian.md/download → .deb 설치  
**Vercel CLI** (배포) | `npm i -g vercel` | Vercel 자동 배포  
**Netlify CLI** (대안) | `npm i -g netlify-cli`  
**Claude API 키**  
1️⃣ `export CLAUDE_API_KEY=YOUR_KEY` 를 `~/.bashrc` 혹은 `~/.zshrc`에 추가 → 터미널 재시작  
2️⃣ VS Code settings.json에 `"claude.apiKey": "YOUR_KEY"` 로도 설정 가능  
📦 4. 프로젝트 초기화 (VS Code ↔ Obsidian)  
# 1️⃣ 프로젝트 루트 생성  
mkdir my-company-website && cd my-company-website  
  
# 2️⃣ npm 초기화 + Vite‑React 템플릿 (초보자용)  
npm create vite@latest . -- --template react  
  
# 3️⃣ 필요한 패키지 설치  
npm install  
  
# 4️⃣ Git 초기화 & GitHub 원격 저장소 연결  
git init  
gh repo create stereo3d‑company‑site --public --source=. --remote=origin  
git add .  
git commit -m "Initial commit: Vite‑React skeleton"  
git push -u origin main  
Obsidian에 같은 폴더 연결  
1. **Obsidian → Settings → Vault → Open folder as vault** 선택  
2. 방금 만든 `my-company-website` 폴더를 선택 → **Vault 이름**을 `Stereo3D‑Site` 로 지정  
3. 이제 **Obsidian → Docs** 폴더 안에서 마크다운을 작성하면 VS Code에서도 바로 보입니다.  
🤖 5. Claude Code와 VS Code 연동  
5‑1. Claude Extension 설정 (VS Code)  
4. **VS Code → Extensions** → “Claude” 검색 → 설치  
5. `Ctrl+Shift+P` → “Claude: Sign In” → API 키 입력 (위에서 export 한 키가 자동 인식)  
5‑2. Claude에게 “마크다운 → 코드” 요청 예시  
**예시 프롬프트** (Obsidian에 `01_회사소개.md` 가 있다고 가정)  
# 01_회사소개.md (Obsidian)  
  
## 우리 회사  
Stereo 3D는 AI‑기반 S3D(스테레오 3D) 컨버팅 서비스를 제공하는 기업입니다.  
...  
**Claude Code**에 전달할 프롬프트:  
/** Claude 명령 */  
convert docs/01_회사소개.md to a React component.  
- Create a file src/pages/About.jsx  
- Use Tailwind CSS for styling  
- Insert the markdown content as JSX (preserve headings, paragraphs, lists)  
- Export default About  
VS Code **Command Palette**(`Ctrl+Shift+P`) → **Claude: Send Prompt** 입력 → 위 텍스트 붙여넣기 → 실행  
Claude가 `src/pages/About.jsx` 파일을 자동으로 생성·삽입합니다.  
[26. 6. 20. 오후 2:07] hermes: **팁**: `// @claude` 같은 주석을 파일에 넣어 두면 “다음에 이 파일을 수정해줘” 라는 지시를 간단히 전달할 수 있습니다.  
5‑3. 반복 흐름 (초보자용)  
단계 | 작업 | Claude 프롬프트 예시  
1️⃣ | **홈 페이지**(Hero) 만들기 | `convert docs/00_개요.md to a React component named HomePage.jsx in src/pages`  
2️⃣ | **서비스 섹션**(Stereo 3D 서비스) | `generate a ServiceCard component for each service described in docs/02_서비스_스테레오3D.md`  
3️⃣ | **포트폴리오**(이미지·동영상) | `create a Portfolio grid using images from public/images/portfolio/* based on docs/04_포트폴리오.md`  
4️⃣ | **공통 레이아웃**(Header/Footer) | `write Header.jsx and Footer.jsx using company logo from public/images/logo.png`  
5️⃣ | **라우팅** | `setup React Router with routes /, /about, /services, /portfolio, /contact`  
6️⃣ | **배포 스크립트** | `add a vercel.json file for zero‑config deployment`  
🚀 6. GitHub CI/CD (자동 배포)  
6‑1. Vercel에 연동 (간단)  
1. **Vercel** 사이트에 로그인 → **New Project** → **GitHub repo** 연결 (`stereo3d-company-site`)  
2. **Framework Preset** → `Vite` 자동 감지 → **Deploy** 클릭  
3. 이후 `git push` 하면 Vercel이 자동으로 빌드·배포한다.  
6‑2. GitHub Actions (옵션)  
`.github/workflows/deploy.yml`  
name: Deploy to Vercel  
  
on:  
push:  
branches: [main]  
  
jobs:  
build-deploy:  
runs-on: ubuntu-latest  
steps:  
- uses: actions/checkout@v3  
  
- name: Setup Node  
uses: actions/setup-node@v3  
with:  
node-version: '20'  
  
- name: Install dependencies  
run: npm ci  
  
- name: Build  
run: npm run build  
  
- name: Deploy to Vercel  
uses: amondnet/vercel-action@v20  
with:  
vercel-token: ${{ secrets.VERCEL_TOKEN }}  
vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}  
vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}  
working-directory: .  
prod: true  
**Secrets**(Settings → Secrets) 에 `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` 를 저장한다.  
📚 7. Obsidian → VS Code 협업 흐름 (실시간 관리)  
1. **Obsidian**에 `docs/` 폴더 안에 **마크다운**을 작성  
2. **VS Code**는 같은 폴더를 열어 두면 파일이 자동 업데이트 → **Claude Code**는 언제든 최신 마크다운을 읽을 수 있다.  
3. **Claude Code에 지시** (VS Code Command Palette 혹은 CLI)  
4. **Obsidian Git 플러그인** (옵션)  
5. **볼트(노트) → 코드** 매핑 예시  
📦 8. 전체 작업 흐름 요약 (초보자용 체크리스트)  
단계 | 수행 내용 | 명령/도구  
**①** | **프로젝트 폴더·Git 초기화** | `mkdir my-company-website && cd $_ && npm create vite@latest . -- --template react && git init && gh repo create …`  
**②** | **Obsidian 볼트 연결** | VS Code와 같은 폴더 열고 → _Obsidian → Open folder as vault_  
**③** | **Claude Code 설치·API키 설정** | `npm i -g @anthropic/claude-code` → `export CLAUDE_API_KEY=…`  
**④** | **콘텐츠 마크다운 작성** | `docs/00_개요.md`, `docs/01_회사소개.md` 등  
**⑤** | **Claude에 코드 생성 요청** | VS Code > Command Palette > _Claude: Send Prompt_ (예시 프롬프트)  
**⑥** | **코드 리뷰·수정** | 생성된 `src/` 파일을 VS Code에서 직접 열어 검토  
**⑦** | **Git 커밋·푸시** | `git add . && git commit -m "Add homepage sections"` → `git push`  
**⑧** | **Vercel 연동·자동 배포** | Vercel 사이트에서 GitHub 리포 연동 → 배포 확인  
**⑨** | **콘텐츠 업데이트** | Obsidian에서 마크다운 수정 → Claude에게 “재생성해줘” 명령 → Git 커밋 → 자동 배포  
📌 9. 실전 예시 프롬프트 모음 (Claude Code에 바로 사용)  
목적 | 프롬프트 (복사‑붙여넣기)  
[26. 6. 20. 오후 2:07] hermes: | **홈 페이지** | `convert docs/00_개요.md to a React component named Home.jsx. Use Tailwind CSS, include a hero image from public/images/hero.jpg` | | **서비스 리스트** | `create a ServiceCard component for each bullet point in docs/02_서비스_스테레오3D.md. Export them from src/components/ServiceCard.jsx` | | **포트폴리오 그리드** | `generate a responsive Portfolio component that loads all images from public/images/portfolio/*.jpg based on docs/04_포트폴리오.md` | | **Header / Footer** | `write Header.jsx with company logo (public/images/logo.png) and navigation links (Home, About, Services, Portfolio, Contact). Write Footer.jsx with copyright text.` | | **루팅** | `setup React Router in src/App.jsx with routes: / → Home, /about → About, /services → Services, /portfolio → Portfolio, /contact → Contact` | | **Tailwind 설정** | `create tailwind.config.cjs enabling custom colors #0A0A0A (dark) and #FF6600 (accent) and enable the typography plugin` | | **배포용 Vercel 설정** | `add a vercel.json that sets the build output directory to 'dist' and rewrites all routes to index.html for SPA` | | **컨텐츠 업데이트** | `update src/pages/About.jsx to reflect the new paragraph added in docs/01_회사소개.md` |  
**작성 팁** – 프롬프트 앞에 `// @claude` 같은 주석을 넣으면 **Claude Code**가 “다음 작업을 수행해 주세요” 라는 뜻을 바로 인식합니다.  
📂 10. Obsidian 볼트 상세 설정 (초보자용)  
6. **폴더 구조** (위에서 만든 `my-company-website` 안)  
7. **Obsidian 플러그인 추천**  
8. **노트 작성 규칙** (Claude에게 일관된 정보 전달을 위해)  
9. **Claude와 연동하는 워크플로**  
5️⃣ **Git** → `git add . && git commit -m "Add new service cards"` → `git push` → **Vercel** 자동 배포  
🎉 11. 마무리 체크포인트  
[ ] **Node**와 **VS Code**가 정상 설치돼 있나요? (`node -v`, `code --version`)  
[ ] **Claude API 키**를 `export CLAUDE_API_KEY=…` 로 환경 변수에 넣었나요?  
[ ] **Obsidian**에 `my-company-website` 폴더를 **Vault** 로 열었나요?  
[ ] **docs/** 안에 기본 마크다운 파일들을 작성했나요? (`00_개요.md` 등)  
[ ] **Claude Code**에 `"convert docs/00_개요.md to a React component …"` 프롬프트를 보냈나요?  
[ ] **GitHub**에 리포가 만들고 **Vercel**에 연결했나요? (`vercel deploy`)  
위 체크리스트를 하나씩 완료하면 **회사 홈페이지**가 **AI‑코드 생성** + **Obsidian 기획 관리** + **자동 배포** 로 완성됩니다.  
🙋‍♀️ 언제든 물어보세요!  
- **프롬프트 수정** 방법  
- **Tailwind** 혹은 **React** 기초 문법  
- **Obsidian Git** 설정 상세  
필요한 부분을 알려 주시면 더 구체적인 코드를 바로 제공해 드릴게요. 🚀